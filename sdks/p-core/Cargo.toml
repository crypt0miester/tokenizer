[package]
name = "p-core"
version = "0.2.0"
edition = "2021"

[dependencies]
pinocchio = { version = "0.10.2", features = ["cpi"] }

[features]
default = []

[lib]
crate-type = ["lib"]
name = "p_core"
